from setuptools import setup, find_packages
setup(
    author = 'DSsoli (SH)',
    description = ' - ',
    name = 'sh_textmaster',
    packages = find_packages(include=['sh_textmaster', 'sh_textmaster.*']),
    version = '0.0.1'
)
